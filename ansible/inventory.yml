all:
  hosts:
    backend_server:
      ansible_host: 13.216.200.51
      ansible_user: ec2-user
      ansible_ssh_private_key_file: ~/.ssh/promptly-ai.pem

    frontend:
      hosts:
        frontend_server:
          ansible_host: 52.21.46.81
          ansible_user: ec2-user
          ansible_ssh_private_key_file: ~/.ssh/promptly-ai.pem
  children:
    backend:
      hosts:
        backend_server
    frontend:
      hosts:
        frontend_server